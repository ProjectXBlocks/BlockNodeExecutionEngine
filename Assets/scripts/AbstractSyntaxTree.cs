using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbstractSyntaxTree : MonoBehaviour
{
    // Expression 은 수식이라는 뜻으로 하나 이상의 값으로 표현될 수 있는 코드 (평가가 가능해서 하나의 값으로 환원됨
    // Statment  ‘진술’, ‘서술’의 의미로 프로그래밍에서는 실행가능한(executable) 최소의 독립적인 코드 조각을 일컫는다

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
